# TASK-0132 Hook, range, floor, and removal tests

Role: Inspector. Round R-0103, wave CTG-0103. Records: ADR-GOV-0018, ADR-REL-0027.
Upstream: TASK-0131 has landed the policy.

Use the disposable-repository helpers already used in
`tests/contract/commit-autofix.test.ts`.

1. `tests/contract/commit-grammar-hook.contract.test.ts`: a subject outside the
   grammar is rejected by `.githooks/commit-msg` with the type list in the
   message; a valid subject passes; staging a `product/` path with a `packages/`
   path is rejected by `.githooks/pre-commit` naming both classes and suggesting
   a split; a `docs:` commit touching `packages/` is rejected.
2. `tests/contract/commit-range-hygiene.contract.test.ts`: build a branch with one
   mixed commit made with `--no-verify`; run `scripts/check-commit-range.mjs
   <base> <head>` and assert it names the commit; assert a merge commit and a
   revert pass.
3. `tests/contract/version-bump-floor.contract.test.ts`: base manifest 1.5.6,
   candidate 1.5.7 with a `feat` commit blocks with `bump-floor-minor`; candidate
   1.6.0 passes; a `!` commit with candidate 1.6.0 blocks with `bump-floor-major`;
   docs-only with no bump passes.
4. `packages/cli/tests/unit/check-adapters-changeset-removed.test.ts`: requesting
   member `changeset-version` is an unknown-member error, never pass or skip.

Do not edit hooks, scripts, or source.

## Acceptance

```bash
pnpm run test:root
pnpm run test:cli
```

## Commits

`test(commit-grammar): ...` in one commit.
