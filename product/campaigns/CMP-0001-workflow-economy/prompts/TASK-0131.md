# TASK-0131 Commit grammar policy and bump floor

Role: Architect. Round R-0103, wave CTG-0103. Records: ADR-GOV-0018 and the
Tier: architect (resolve the host model from `models.tiers` in campaign.json). Effort: high. Time budget: 90 minutes.
policy half of ADR-REL-0027.

## Steps

1. `law/schemas/commit-grammar.schema.json` and `law/policy/commit-grammar.json`:
   subject pattern `^(type)(\(scope\))?(!)?: .+$` with types feat, fix,
   refactor, perf, test, docs, ci, build, chore, law, spec, plan. Per type:
   `allowed_classes` from `law/policy/change-taxonomy.json` and `bump_floor` in
   none, patch, minor. A breaking marker maps to major regardless of type.
   Families and pairings are referenced from the taxonomy, not restated.
   `exemptions`: merge commits and reverts inherit classification.
   `historical_cutoff`: the commit after which the range check applies; explain
   in the description that the maintainer fills it with the merge commit of this
   round's Engineer pull request.
2. Add the schema name to `SOURCE_ONLY_SCHEMAS` in
   `packages/cli/src/commands/check/schemas.ts`.
3. `AGENTS.md`: one bullet stating the grammar and the single-family rule,
   pointing at the policy.
4. `docs/dev/contributing.md`: a short section with two examples of a correct split.

## Acceptance

```bash
node .devai/state/pr-bootstrap/cli/bin.js check --only schemas --format json
node .devai/state/pr-bootstrap/cli/bin.js doctor --format json
```

## Commits

`law(commit-grammar): ...`; `docs(agents): ...`; `docs(dev): ...`. Three commits.
Do not create hooks or scripts.
