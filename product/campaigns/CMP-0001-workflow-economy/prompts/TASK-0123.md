# TASK-0123 Class selector, taxonomy member, class-derived release intent

Role: Engineer. Round R-0102, wave CTG-0102. Record: ADR-GOV-0017.
Upstream: TASK-0121 and TASK-0122 have landed. Make the tests green without
weakening them.

## Steps

1. `packages/cli/src/services/change-taxonomy.ts`: load and validate the law
   policy and the adopter binding; expose `classify(path)` and `loadBindings()`
   throwing `CHANGE_TAXONOMY_BINDING_OVERLAP` and `CHANGE_TAXONOMY_CLASS_UNKNOWN`.
2. `packages/cli/src/services/check-runner/policy.ts`: extend `selectorMatches`
   so kind `class` matches when `classify(path)` equals the pattern. Keep exact,
   prefix, and glob unchanged. Locate it with `graft grep "selectorMatches"`.
3. `packages/cli/src/commands/check/adapters.ts`: add member `change-taxonomy`
   that classifies `git ls-files` and fails naming every unclassified path.
4. `scripts/run-pr-release-gate.mjs`: replace the `documentationOnly`,
   `metadataOnly`, and `risks` regular expressions with the class set of the
   changed paths. Keep the release intent output shape unchanged.
5. `test-tasks.json`: add nodes `plan:validate`, `law:validate`, `docs:validate`
   selecting by class and running only existing validation members (adrs,
   schemas, docs-links, trace), so classified paths never reach `test:local-full`.
6. Regenerate `docs/reference/cli/check-suites.md` with
   `node docs/reference/cli/render-generated-reference.mjs --write` so the new
   member appears.
7. Report the new task-policy digest in your final message; the maintainer
   re-issues the attestation.

## Acceptance

```bash
pnpm run test:cli
pnpm run test:root
pnpm run test-task-selectors:check
node .devai/state/pr-bootstrap/cli/bin.js check --only change-taxonomy --format json
```

## Commits

`feat(taxonomy): ...` for the service, policy, adapter, and gate;
`ci(test-tasks): ...` for the descriptor. Do not touch tests or law.
