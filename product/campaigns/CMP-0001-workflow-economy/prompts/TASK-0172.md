# TASK-0172 Backlog store, action, surface, and projection tests

Role: Inspector. Round R-0105, wave CTG-0107. Record: ADR-GOV-0019.
Upstream: TASK-0171 has landed the registry entries and schemas.

1. `packages/loop/tests/unit/repository-backlog-store.test.ts`: add, list, show,
   resolve over a temporary repository; items validate against
   `backlog-item.schema.json`; ids are allocated from `.devai/state/counters.json`.
2. `packages/cli/tests/unit/backlog-actions.test.ts`: every backlog action
   succeeds with the network boundary denied (follow the pattern in
   `packages/cli/tests/unit/canonical-workflow-success-acceptance.test.ts`), and
   the authority host effects recorded are filesystem writes under
   `.devai/state/backlog` only.
3. `packages/cli/tests/unit/doctor-backlog-surface.test.ts`: create an item, clone
   the repository at the same commit, run doctor, and assert the open item is
   listed with no host-specific state.
4. `packages/cli/tests/unit/tracking-backlog-projection.test.ts`: projection
   without an activation refuses; with an activation it emits
   `backlog_item_projected` under the disclosure profile; a backlog action given
   a gap id does not change the gap.

Do not edit source.

## Acceptance

```bash
pnpm run test:loop
pnpm run test:cli
```

## Commits

`test(backlog): ...` in one commit.
