# TASK-0163 Rung resolution in the kernel, channel script, and release workflow

Role: Engineer. Round R-0105, wave CTG-0106. Record: ADR-REL-0028.
Upstream: TASK-0161 and TASK-0162 have landed. Make the tests green.

## Steps

1. `packages/cli/src/services/release-profile.ts` holds the plan kernel; the
   preflight binding is in `release-preflight.ts`. Resolve the rung from the prerelease identifier,
   validate promotion order against the previous version, add the rung's required
   capabilities, and block on a `channel` mismatch. Bind the channel into the preflight
   receipt expectations in `release-preflight.ts`.
2. `scripts/release-channel.mjs`: emit `channel` and the rung `dist_tag` from the
   policy instead of the fixed `next`. `scripts/create-release-manifest.mjs`
   consumes the new field.
3. `.github/workflows/release.yml`: publish to the rung dist-tag only; keep the
   existing dist-tag verification step.
4. Update `docs/reference/scripts.md`.

## Acceptance

```bash
pnpm run test:cli
pnpm run test:root
pnpm run typecheck
node scripts/check-workflows.mjs
```

## Commits

`feat(release): ...`; `ci(workflows): ...`; `docs(reference): ...`.
