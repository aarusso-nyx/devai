# TASK-0182 Replay, refusal, prompt validation, and no-terminal tests

Role: Inspector. Round R-0106, wave CTG-0108. Record: ADR-CFG-0001.
Tier: worker (resolve the host model from `models.tiers` in campaign.json). Effort: medium. Time budget: 60 minutes.
Upstream: TASK-0181 has landed the contract page.

Drive the flow with scripted standard input; follow the subprocess pattern used
in `packages/cli/tests/unit/cli-shard09-init-core-planning.test.ts`.

1. `packages/cli/tests/unit/init-interactive-replay.test.ts`: complete a bind
   session in a temporary target; assert every filesystem write is attributable
   to an `init bind` or `init apply` invocation recorded in the authority host
   effects, and that running the printed argv non-interactively in a second
   temporary target yields the same plan bytes.
2. `packages/cli/tests/unit/init-interactive-materialized-refusal.test.ts`: in
   edit mode, selecting `thresholds.json` is refused with a message offering
   `init bind`, and the file is unchanged.
3. `packages/cli/tests/unit/init-interactive-schema-prompts.test.ts`: answering
   `tier4` to the profile prompt is rejected with the schema enumeration message
   before any plan is built.
4. `packages/cli/tests/unit/init-interactive-no-tty.test.ts`: with standard input
   closed, the command exits with a structured error envelope and the target tree
   is unchanged.

Do not edit source.

## Acceptance

```bash
pnpm run test:cli
```

## Commits

`test(init): ...` in one commit.
