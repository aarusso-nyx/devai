# TASK-0191 Roll the candidate to 1.6.0 and write the changelog

Role: Engineer. Round R-0107, wave CTG-0109. Record: ADR-REL-0027.
Tier: worker (resolve the host model from `models.tiers` in campaign.json). Effort: medium. Time budget: 45 minutes.
Upstream: rounds R-0101 to R-0106 are closed on main.

## Steps

1. Replace every occurrence of the current candidate version (read it from
   `packages/cli/package.json`) with `1.6.0` at the same sites the 1.5.7
   rollover touched: `package.json`, `packages/cli/package.json`,
   `docs/site/src/pages/index.tsx`, and the pinned fixtures in
   `packages/cli/tests/unit/version.test.ts`,
   `release-package-staging.test.ts`, `adopter-package-contract.test.ts`,
   `cli-shard09-init-bind-adapters-policy.test.ts`,
   `doctor-adopter-policy-binding.test.ts`,
   `doctor-check-result-identities.test.ts`, and
   `doctor-observable-boundaries.test.ts`. Find them with
   `git log -1 --stat 35050687` and `grep -rn "1\.5\.7"`.
2. `CHANGELOG.md`: add a `## 1.6.0 — <today>` section with one line per merged
   record, in the order of `product/campaigns/CMP-0001-workflow-economy/campaign.json`.
   Read each round's records and the pull requests recorded on its tasks.
3. Do not change the trusted local-RC verifier provider pin or any workflow.

## Acceptance

```bash
pnpm run release:static-integrity
pnpm run release:closure
pnpm run test:cli
```

## Commits

`chore(release): roll candidate to 1.6.0` in one commit covering every site.
