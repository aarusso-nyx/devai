# TASK-0161 Prerelease rung table and intent channel field

Role: Architect. Round R-0105, wave CTG-0106. Record: ADR-REL-0028.

## Steps

1. `law/policy/release-lifecycle.json` and
   `law/schemas/release-lifecycle-policy.schema.json`: add a `prerelease_ladder`
   section under `plan_determination`: rungs alpha, beta, rc in order, each with
   `dist_tag`, `required_capabilities` (alpha: unconditional floor plus
   affected-checks and dependent-checks; beta: plus unit and integration; rc: the
   complete RC coverage closure), `promotion` rules (forward or same-rung
   suffix increment; anything else is a downgrade), and `stable_from` equal to
   rc only. Keep the kernel identifier unchanged and say why in the policy.
2. `law/schemas/release-intent.schema.json`: optional `channel` with enum alpha,
   beta, rc, stable; the kernel blocks when it disagrees with the version string.
3. `docs/dev/operations/versioning-policy.md` and
   `docs/adopters/release-profiles.md`: document the ladder and the dist-tags.

## Acceptance

```bash
pnpm run test:schemas
node .devai/state/pr-bootstrap/cli/bin.js check --only schemas --format json
```

## Commits

`law(release): ...`; `docs(operations): ...`; `docs(adopters): ...`.
Do not edit scripts or CLI source.
