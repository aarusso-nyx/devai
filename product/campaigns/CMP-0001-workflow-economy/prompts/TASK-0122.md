# TASK-0122 Selection, classification, and gate derivation tests

Role: Inspector. Round R-0102, wave CTG-0102. Record: ADR-GOV-0017.
Tier: worker-high (resolve the host model from `models.tiers` in campaign.json). Effort: high. Time budget: 90 minutes.
Upstream: TASK-0121 has landed the policy, schema, bindings, and selector kind.

Write tests that are red now and green after TASK-0123.

1. `packages/cli/tests/unit/check-runner-class-selector.test.ts`: a node with
   `{kind: "class", pattern: "plan"}` is selected when a bound plan path changes
   and not when a code path changes; a path no binding covers still widens to
   `fallbackNodeId`; a plan-only change plans no node whose runner is
   `vitest-v1`, `vitest-coverage-v1`, or a build script.
2. `packages/cli/tests/unit/check-taxonomy-member.test.ts`: the
   `change-taxonomy` member fails naming the unclassified path; fails at load on
   overlapping bindings; fails on a class outside the law enum.
3. `tests/contract/pr-release-intent-classes.contract.test.ts`: extend the pattern
   of `tests/contract/pr-release-intent-paths.test.ts` so the gate script derives
   `change_kind` documentation for docs-only, metadata for plan-only and law-only
   without registry changes, behavioral for code, and risks from classes.

Do not edit `packages/cli/src` or `scripts/`.

## Acceptance

```bash
pnpm run test:cli
pnpm run test:root
```

## Commits

`test(taxonomy): ...` in one commit.
