# TASK-0142 BLOCKED semantics, cache, parity, and redaction tests

Role: Inspector. Round R-0104, wave CTG-0104. Record: ADR-CHK-0001.
Tier: worker-high (resolve the host model from `models.tiers` in campaign.json). Effort: high. Time budget: 90 minutes.
Upstream: TASK-0141 has landed the probe schema and descriptor changes.

Follow the fixture pattern of `packages/cli/tests/unit/check-runner.test.ts` and
`packages/cli/tests/unit/check-cache-malformed-index.test.ts`.

1. `packages/cli/tests/unit/check-runner-preflight-blocked.test.ts`: a descriptor
   with one extrinsic `registry` probe pointed at an unreachable host and one
   dependent vitest node plans and runs to probe outcome `BLOCKED`, dependent
   state `blocked-environment`, no execution of the dependent, and no FAIL entry
   under the cache root. An intrinsic probe failure yields `FAIL` and is cached
   as such.
2. `packages/cli/tests/unit/check-cache-blocked-never-reusable.test.ts`: after a
   BLOCKED outcome, a second plan with identical inputs reports `execute` for the
   probe, never `reusable`.
3. `packages/cli/tests/unit/preflight-diagnostics-redaction.test.ts`: a failing
   probe whose observed value contains a distinctive marker produces diagnostics
   through `scripts/pr-failure-diagnostics.mjs` that do not contain the marker.
4. `tests/contract/preflight-lane-parity.contract.test.ts`: parse
   `.github/workflows/pull-request-checks.yml`, extract the check invocations,
   run the same invocations with `--task-plan` locally against the same base and
   candidate, and assert the planned node sets are byte-identical.

Do not edit source or the descriptor.

## Acceptance

```bash
pnpm run test:cli
pnpm run test:root
```

## Commits

`test(preflight): ...` in one commit.
