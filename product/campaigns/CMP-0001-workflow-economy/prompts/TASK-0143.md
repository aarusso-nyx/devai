# TASK-0143 Preflight runner kind, BLOCKED outcome, probe nodes, collapsed lane

Role: Engineer. Round R-0104, wave CTG-0104. Record: ADR-CHK-0001.
Upstream: TASK-0141 and TASK-0142 have landed. Make the tests green.

## Steps

1. `packages/cli/src/services/check-runner/types.ts`: add `BLOCKED` to
   `TaskOutcome` and a `blocked-environment` state for dependents.
2. `packages/cli/src/services/check-runner/preflight.ts`: execute each probe kind
   from the descriptor. `git` probes: base fetched and merge-base equals base
   head, clean tree. `file`: lockfile frozen consistency, materialization current,
   constitution pin, generated views current. `toolchain`: compare with
   `.devai/config/toolchain.json`. `registry`: verifier provenance reachable and
   pinned. `command`: taxonomy classification complete, commit range hygiene.
   `credential`: delegate to the service that CTG-0105 adds; until then, report
   `skipped`. Observed values pass through `redact` from `@devai-nyx/utils`.
3. `runner.ts` and `cache.ts`: an extrinsic failure writes no result digest, marks
   dependents blocked-environment, and the report aggregates blocked separately
   from failed. `policy.ts`: select preflight nodes for every target.
4. `test-tasks.json`: add the probe nodes with runner `preflight-v1` as the root
   of the DAG.
5. `.github/workflows/pull-request-checks.yml` and `scripts/run-pr-release-gate.mjs`:
   reduce to install, `check --preflight --run --base <base>`, then the affected
   and profile runs. Delete the step-level aggregator. Keep
   `scripts/check-workflows.mjs` green; extend `scripts/pr-failure-diagnostics.mjs`
   to render blocked probes with their remediation.
6. Report the new task-policy digest.

## Acceptance

```bash
pnpm run test:cli
pnpm run test:root
pnpm run typecheck
node scripts/check-workflows.mjs
```

## Commits

`feat(preflight): ...`; `ci(test-tasks): ...`; `ci(workflows): ...`;
`docs(reference): ...`. Do not touch tests or law.
