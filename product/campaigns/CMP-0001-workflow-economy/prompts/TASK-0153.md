# TASK-0153 Credential probe, workflow bijection, scan, prerequisites job

Role: Engineer. Round R-0104, wave CTG-0105. Record: ADR-SEC-0001.
Upstream: TASK-0151 and TASK-0152 have landed. Make the tests green.

## Steps

1. `packages/cli/src/services/credential-probe.ts`: one implementation. For
   `gh-auth` run `gh auth status` and parse scopes; for the package registry run
   `npm whoami --registry`; for environment kinds check presence and shape only.
   Never log or return a value; return present, absent, scope-insufficient, or
   expired with the manifest id. Wire it into `doctor.ts` as one check and into
   `check-runner/preflight.ts` as the `credential` probe kind.
2. `scripts/check-workflows.mjs`: enforce the bijection between `secrets.*`
   references in workflows and manifest entries with `kind` repository-secret or
   environment-secret.
3. Generalize the token-shape scan in
   `packages/cli/src/services/github-issues-tracking/config.ts` into a shared
   helper over committed configuration and call it from the static integrity
   path that backs `release:static-integrity`.
4. `scripts/process/release-prerequisites.mjs` and `.github/workflows/release.yml`:
   make the prerequisites probe the first job; every other job `needs` it.
   Mirror the same first job in `devai-ledger-verify.yml`.
5. Update `docs/reference/scripts.md`.

## Acceptance

```bash
pnpm run test:root
pnpm run test:cli
pnpm run typecheck
node scripts/check-workflows.mjs
```

## Commits

`feat(credentials): ...`; `ci(workflows): ...`; `docs(reference): ...`.
