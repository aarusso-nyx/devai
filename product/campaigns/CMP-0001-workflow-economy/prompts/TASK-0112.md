# TASK-0112 Toolchain manifest consumer tests

Role: Inspector. Round R-0101, wave CTG-0101. Record: ADR-CHK-0002.
Tier: worker (resolve the host model from `models.tiers` in campaign.json). Effort: medium. Time budget: 60 minutes.
Upstream: TASK-0111 has landed the schema, `.devai/config/toolchain.json`, and
the adopter default.

Write tests that are red now and green once TASK-0113 lands. Use vitest as
configured in `tests/config/local.config.ts`.

1. `tests/contract/toolchain-manifest.contract.test.ts`: the committed manifest
   validates against its schema; every pin constant exported by
   `scripts/check-workflows.mjs` equals the manifest value; every `node-version`
   in `.github/workflows/*.yml` equals the manifest node major.
2. `tests/contract/check-workflows-manifest.contract.test.ts`: copy the workflows
   to a temporary directory, change one action digest, run `checkWorkflowTree`
   from `scripts/check-workflows.mjs` against it, and assert one finding naming
   the file, the manifest key, the observed digest, and the required digest.
   Repeat for a changed node major and for `EXPECTED_ACTION_COUNT`. Assert the
   provisioning script exits with `DEVAI_TOOLCHAIN_MANIFEST_REQUIRED` when the
   manifest path in its controls file is absent.
3. `packages/cli/tests/unit/check-runner-toolchain-digest.test.ts`: following the
   fixture pattern in `packages/cli/tests/unit/check-runner.test.ts`, assert that
   two plans differing only in manifest bytes produce different toolchain
   digests and task keys, and identical manifests produce equal keys.

Cover each item in the record's Inspector Adversarial Acceptance section.
Do not modify source under `packages/` or `scripts/`.

## Acceptance

```bash
pnpm run test:root
pnpm run test:cli
```

## Commits

`test(toolchain): ...` in one commit.
