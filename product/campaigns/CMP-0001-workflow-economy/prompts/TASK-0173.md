# TASK-0173 Backlog store, actions, doctor surface, projection

Role: Engineer. Round R-0105, wave CTG-0107. Record: ADR-GOV-0019.
Tier: worker-high (resolve the host model from `models.tiers` in campaign.json). Effort: high. Time budget: 120 minutes.
Upstream: TASK-0171 and TASK-0172 have landed. Make the tests green.

## Steps

1. `packages/loop/src/repository-backlog/index.ts`: schema-validated store under
   `.devai/state/backlog/<id>.json`, counters from `.devai/state/counters.json`
   (add `BL`), functions add, list, show, resolve. Export from
   `packages/loop/src/index.ts`.
2. `packages/cli/src/commands/backlog/index.ts`: register the four actions with
   `defineCommand` and the registry contracts; wire into
   `packages/cli/src/command-manifest.ts`. Optional `--round` is passed through,
   never inferred.
3. `packages/cli/src/commands/doctor.ts`: one check listing open items.
   `packages/cli/src/commands/round/workflow.ts`: `round plan` surfaces open
   items in its payload.
4. `packages/cli/src/services/github-issues-tracking/projector.ts`: emit
   `backlog_item_projected` under the existing activation and disclosure rules.
5. Regenerate `docs/reference/cli/index.md` with
   `node docs/reference/cli/render-generated-reference.mjs --write`.

## Acceptance

```bash
pnpm run test:loop
pnpm run test:cli
pnpm run typecheck
node .devai/state/pr-bootstrap/cli/bin.js check --only action-effects --format json
node .devai/state/pr-bootstrap/cli/bin.js check --only cli-reference --format json
```

## Commits

`feat(backlog): ...`; `docs(reference): ...`.
