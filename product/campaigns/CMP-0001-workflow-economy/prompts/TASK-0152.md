# TASK-0152 Credential bijection, probe redaction, scan, prerequisites-job tests

Role: Inspector. Round R-0104, wave CTG-0105. Record: ADR-SEC-0001.
Upstream: TASK-0151 has landed the schema and manifest.

1. `tests/contract/check-workflows-credentials.contract.test.ts`: add a
   `secrets.X` reference to a copied workflow without a manifest entry and assert
   a finding; remove a referenced entry from a copied manifest and assert the
   reverse finding.
2. `packages/cli/tests/unit/doctor-credential-probe.test.ts`: with a stubbed
   subprocess boundary, a probe over a credential whose fake value contains a
   distinctive marker reports one of present, absent, scope-insufficient, or
   expired, and no output stream, report field, or diagnostics file contains the
   marker.
3. `tests/contract/credential-scan.contract.test.ts`: a committed configuration
   file outside the tracking binding containing a `ghp_` shaped literal fails
   `release:static-integrity`.
4. `tests/contract/release-prerequisites-job.contract.test.ts`: parse
   `.github/workflows/release.yml` and assert the first job runs
   `scripts/process/release-prerequisites.mjs` and every other job needs it.

Do not edit source, scripts, or workflows.

## Acceptance

```bash
pnpm run test:root
pnpm run test:cli
```

## Commits

`test(credentials): ...` in one commit.
