# TASK-0162 Ladder kernel and channel tests

Role: Inspector. Round R-0105, wave CTG-0106. Record: ADR-REL-0028.
Upstream: TASK-0161 has landed the ladder policy and schema.

1. `packages/cli/tests/unit/release-plan-prerelease-ladder.test.ts`: using the
   existing release plan fixtures (find them with `graft grep "release plan"` in
   `packages/cli/tests`), assert `1.6.0-nightly.1` blocks with invalid-semver
   precedence; `1.6.0-beta.1` after `1.6.0-rc.1` blocks as downgrade;
   `1.6.0-alpha.2` after `1.6.0-alpha.1` passes; `1.6.0` from `1.6.0-beta.3`
   blocks; `1.6.0` from `1.6.0-rc.1` passes; a `channel` disagreeing with the
   version blocks.
2. `tests/contract/release-channel-ladder.contract.test.ts`: `releaseChannel`
   maps each rung to its dist-tag, stable to `latest`, and refuses to resolve an
   alpha to `latest`.

Do not edit source or scripts.

## Acceptance

```bash
pnpm run test:cli
pnpm run test:root
```

## Commits

`test(release): ...` in one commit.
