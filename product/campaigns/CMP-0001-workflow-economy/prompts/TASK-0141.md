# TASK-0141 Preflight probe contract and BLOCKED outcome

Role: Architect. Round R-0104, wave CTG-0104. Record: ADR-CHK-0001.

## Steps

1. `law/schemas/preflight-probe.schema.json` (meta gate rules as for every
   schema): `id`, `class` (extrinsic, intrinsic), `probe` with `kind`
   (environment, file, command, git, registry, toolchain, credential) and
   kind-specific parameters, `expected`, `observed` (redacted string or null),
   `status` (pass, fail, blocked, skipped), `remediation`, `depends_on`.
   Describe in the schema that an extrinsic failure yields blocked and is never
   cached, and an intrinsic failure yields fail.
2. `law/schemas/test-task-descriptor.schema.json`: allow runner `preflight-v1`
   whose node carries a `probes` array of probe objects instead of an `argv`
   contract; document `BLOCKED` as the fifth task outcome.
3. Catalogue the new schema in `SOURCE_ONLY_SCHEMAS` in
   `packages/cli/src/commands/check/schemas.ts`.
4. `docs/adopters/test-tasks.md`: a "Preflight probes" section with one example
   node. `docs/dev/operations/remote-preflight-contract.md`: update the lane
   description to install, preflight check, affected check, and state that the
   step-level aggregator now lives in the runner report.
5. `AGENTS.md`: one sentence requiring the local preflight against a fetched base
   before opening a pull request.

## Acceptance

```bash
node .devai/state/pr-bootstrap/cli/bin.js check --only schemas --format json
node .devai/state/pr-bootstrap/cli/bin.js check --only docs-links --format json
```

## Commits

`law(preflight): ...`; `docs(adopters): ...`; `docs(operations): ...`;
`docs(agents): ...`. Do not edit `test-tasks.json` or runner source.
