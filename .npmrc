# @aarusso-nyx packages publish to / resolve from GitHub Packages. Auth enters
# only through NODE_AUTH_TOKEN: write:packages for publishing, read:packages for
# consumers.
@aarusso-nyx:registry=https://npm.pkg.github.com
//npm.pkg.github.com/:_authToken=${NODE_AUTH_TOKEN}
